namespace Server;

public class Packet
{
    //NetworkEvents
    //NetworkConnection(NetworkClient);
    //NetworkReceive(NetworkClient, byte[]);
    
    //NetworkServer
    // Socket, Endpoint, new Socket, Socket.Bind, Socket.Listen, BeginAccept, EndAccept
    
    //NetworkClient
    //Socket from NetworkServer.EndAccept, NetworkServer for calling events, byte[] Buffer with bufferSize
    //BeginReceive, EndReceive, BeginSend, EndSend, Disconnect
}